services:
  botttb:
    build: .
    container_name: botttb-moderator
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
